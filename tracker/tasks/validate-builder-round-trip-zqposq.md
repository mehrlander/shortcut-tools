---
id: validate-builder-round-trip-zqposq
title: Validate that a generated shortcut imports on device
status: backlog
opened: 2026-08-09
---
# Validate that a generated shortcut imports on device

Nothing in this repo demonstrates that `Shortcut.export()` produces a file the
Shortcuts app will accept. The builder emits a plausible plist, and structural
validity is not importability: a file can be well-formed plist and still carry a
wrong identifier, an unpaired control-flow block, or a missing key the app
requires.

Specific things a round trip would settle, none of which is currently known:

- Whether the hardcoded `WFWorkflowMinimumClientVersion` of 900 and
  `WFWorkflowClientVersion` of `2302.0.4` are accepted by current iOS.
- Whether `WFWorkflowTypes: ["NCWidget", "WatchKit"]`, set on every shortcut the
  builder emits, is right as a default.
- Whether a shortcut built entirely through the control-flow helpers imports
  with its blocks intact.

The method that worked before was to generate the plist, stamp the pasteboard
UTI through the Actions app, and paste into the editor. That route is documented
in the `apple-shortcuts-actions` skill in `mehrlander/web-tools`.

This task needs a device. It cannot be closed from a sandbox session.

**Done when** a shortcut generated by `shortcut.js`, including at least one
conditional and one menu, has been imported on device and run, and the result
plus any corrections to the builder's defaults are recorded here.

## Progress log
- 2026-08-09: Filed. The builder's hardcoded version and type defaults are in
  `shortcut.js` `build()`.
